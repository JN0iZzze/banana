import type { SlideTheme } from '../../presentation/types';

export const themeClassNames: Record<SlideTheme, string> = {
  editorial: 'theme-editorial',
  signal: 'theme-signal',
  cinema: 'theme-cinema',
};

